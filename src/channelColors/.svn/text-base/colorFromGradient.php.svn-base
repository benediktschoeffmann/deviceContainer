<?

// // ------------------------------------------------------------------------

// include 'channelColors.php';

// // ------------------------------------------------------------------------

// foreach ($channelColors as $channel => $colors) {

//     $parts1 = array(hexdec(substr($colorLeft, 0, 2)), hexdec(substr($colorLeft, 2, 2)), hexdec(substr($colorLeft, 4, 2)));
//     $parts2 = array(hexdec(substr($colorRight, 0, 2)), hexdec(substr($colorRight, 2, 2)), hexdec(substr($colorRight, 4, 2)));

//     $colors = array();
//     foreach ($parts1 as $key => $part) {
//         $colors[$key] = dechex(floor($parts2[$key] + (($parts1[$key] - $parts2[$key]) / 2)));
//     }

//     echo var_export(implode($colors), true)."\n";
// }

